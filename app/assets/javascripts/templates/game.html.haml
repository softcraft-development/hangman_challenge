#game-board
  %h1
    Game \#{{ id }}
  %h2
    Category: Countries of the World
  .misses
    %canvas#hangman-display{:width=>"300", :height=>"400", "hangman-display" => ""}
  .word-section
    %h3 Your Guesses:
    .word
      %span.position{"ng-repeat"=>"position in positions track by $index", "ng-class" => "{ 'blank' : position == null,'space' : position == ' ' }"}
        {{ position }}
  .letters
    - Guess::RANGE.each do |letter|
      %button.letter.btn.btn-default{:type=>"button", :name=>"letter_#{letter}", :value => letter, "ng-click"=>"makeGuess('#{letter}')", "ng-class"=>"{ 'guessed' : guesses.indexOf('#{letter}') >= 0 }" } 
        = letter
  .controls
    Done with this game? <a href="/">Return to the game list</a> or <a href="" ng-click="newGame()">start a new one</a>.
